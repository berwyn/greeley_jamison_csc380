To use this client, you need:

- Ruby 1.9.3 or Ruby 2.0.0
- Bundler

(I recommend RubyInstaller for windows, apt for Debian systems)

To get the required gems,

cd [project dir] && bundle install

To view commands, use

ruby app.rb

with no arguments